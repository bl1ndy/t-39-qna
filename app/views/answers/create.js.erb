$('.new-answer-errors').html('<%= render 'shared/errors', object: @answer %>')

<% if @answer.persisted? %>
  $('.answers').append('<%= j render @answer %>')
  $('.new-answer-form #answer_body').val('')
<% end %>
